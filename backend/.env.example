# MongoDB Atlas Connection String
# Get this from your MongoDB Atlas dashboard -> Connect -> Connect your application
MONGODB_URI="mongodb+srv://username:password@cluster.mongodb.net/"

# Optional: Specify database name (if different from default)
MONGODB_DBNAME="your_database_name"

# Server Port (optional, defaults to 5001)
PORT=5001

# Instructions:
# 1. Copy this file to .env
# 2. Replace MONGODB_URI with your actual MongoDB Atlas connection string
# 3. Update MONGODB_DBNAME if needed
# 4. Save and run: npm start
